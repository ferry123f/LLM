# Knowledge Base Index

## DeepSeek

- [DeepSeek-V3 架构与低成本高效训练](deepseek/DeepSeek-V3%20架构与低成本高效训练.md) — DeepSeek-V3（671B / 激活 37B）用 MLA + DeepSeekMoE + 无辅助损失负载均衡 + MTP，配合 FP8 训练与 DualPipe，把完整训练成本压到 2.788M H800 GPU 时（约 $5.576M），性能追平 GPT-4o 与 Claude-3.5-Sonnet，是当时最强开源模型（含论文官方架构图 Fig2/3/6 与核心公式）。Updated: 2026-08-07
- [DeepSeek-V4 架构与百万上下文效率](deepseek/DeepSeek-V4%20架构与百万上下文效率.md) — DeepSeek-V4（Pro 1.6T / Flash 284B）用混合注意力 CSA+HCA、mHC 超连接、Muon 优化器把 1M token 上下文推理成本压到 V3.2 的 10–27%，附完整架构、训练与评测消化（含论文官方架构图 Fig2/3/4 与核心公式）。Updated: 2026-08-12
- [DeepSeek-V4 config.json 逐项详解与显存估算](deepseek/DeepSeek-V4%20config.json%20逐项详解与显存估算.md) — [Archived] 面向初学者：拿 V4-Pro 的 config.json 逐字段放回 Transformer 骨架（骨架/注意力/压缩/MoE/mHC/精度六组），再一步步算出 1.6T 总参、49B 激活、1M 上下文 KV cache 从基线 250 GB 砍到 5.2 GB（≈2%，与论文互证）的完整账，附 Flash 对比速览与两个未解之谜（dspark_* 字段、compress_ratios 长度不符）。Updated: 2026-08-14
- [Hyper-Connections 超连接](deepseek/Hyper-Connections%20超连接.md) — 残差连接的可学习替代（ByteDance, ICLR 2025）：用深度连接 + 宽度连接（扩展率 n）化解 Pre-/Post-Norm 的「梯度消失↔表示崩塌」跷跷板，DHC×4 收敛快 1.8×、ARC +6 分；是 DeepSeek-V4 mHC 的理论基础（含论文 Figure 2 示意图与核心公式）。Updated: 2026-08-12

## 学习整理

- [Docker 常用命令](学习整理/docker.md) — Docker 镜像/容器核心区分 + 镜像/容器/run 参数/日志/清理/Compose 常用命令速查（已纠正镜像与容器混淆）。Updated: 2026-08-10
- [Git 常用命令](学习整理/git.md) — Git 首次配置、初始化关联远程、日常 add/commit/pull/push 上传流程，外加查历史/撤销回退/分支/.gitignore/rebase 冲突处理等高频救急命令速查。Updated: 2026-08-12
- [LLM 推理的 GPU 硬件基础](学习整理/LLM推理的GPU硬件基础.md) — 从「算力/带宽失衡」（H100 约 295 FLOP/Byte）出发讲清 LLM 推理的硬件约束：prefill 是 compute-bound、decode 是 memory-bound；显存四块构成与 KV cache 计算公式（MHA/GQA/MQA/MLA 对比）；单流 decode 速度上限 = 带宽÷权重；内存层级、精度格式、TP/PP/EP 互联代价、常见卡对照与容量规划实例；附 int32 vs int64 的四点收益与速查公式。Updated: 2026-08-13
- [LLM 分布式：并行策略、通信原语与推理侧对策](学习整理/LLM分布式.md) — 从外往里五层：DP（含参数服务器瓶颈与 ZeRO 三级切分）/ DDP+Ring-AllReduce（scatter-reduce + all-gather 两阶段图解，通信量与卡数无关）/ PP（bubble → micro-batch → 1F1B 省显存）/ TP（行并行 All-Reduce vs 列并行 All-Gather，Megatron「列→行」配对省一次通信）/ EP（all-to-all 与负载不均）；八个集合通信原语逐图解释 + 一张表理清「All-Reduce = Reduce-Scatter + All-Gather」这类拆解关系；接回硬件的通信代价表（TP 每层 2 次同步、怕的是延迟不是带宽）；**训练 vs 推理的地位重排**（DP 退化成多副本、PP 因无 micro-batch 可切而失势、TP 靠分摊权重搬运成为主力、ZeRO 基本用不上）；单卡内部 SM/Warp/Block 五步调度与 SIMT；量化 W8A8/AWQ/GPTQ/FP8 对比（AWQ 事前保护 vs GPTQ 事后补偿，W4 与 W8A8 在 decode/prefill 上的分野）；投机采样（draft→verify→accept 三步、白送的 bonus token、四路线谱系 standalone/N-gram/EAGLE/MTP 按「draft 与 target 信息耦合程度」排序、收益随 batch 增大而衰减的三条边界；并澄清 SGLang 里 `NEXTN` 只是 `EAGLE` 的别名、两个 accept-threshold 旋钮会把「分布无损」变成有损档位）；**PD 分离的 KV 传输链路**（NIXL / Mooncake TE：为什么不能复用 NCCL、注册显存→带外交换元数据→单边 write 三步、GPU-Direct RDMA 不占 SM，与集合通信的六项对照）；附 SGLang 参数、`distributed/`、`disaggregation/` 与 `speculative/` 落点。Updated: 2026-09-02
- [LLM 推理压测：bench serve 与 throughput 参数详解](学习整理/LLM推理压测-bench%20serve%20与%20throughput%20参数详解.md) — SGLang 与 vLLM 两框架的在线压测（serve）与离线吞吐（throughput）逐参数详解，含 TTFT/TPOT/ITL/goodput 指标解释、四命令对照表与上手示例；新增 Qwen3-0.6B 四组实测记录与分析：总吞吐被输入灌水（=Output×9）、同命令两跑差 1.68×（warmup/前缀缓存实证）、离线比在线快 35%、ITL Max 1817ms 暴露 prefill 抢占 decode、retokenized 少 20% 揭示 random-ids 代价，附「不可比」五条质疑与复现清单。Updated: 2026-08-13
- [国产 GPU/NPU 适配：生态、路径与 SGLang 实证](学习整理/国产GPU与NPU适配.md) — 以 SGLang 源码为证据评估国产 AI 芯片的框架适配现状：三个适配落点（设备探测 / `hardware_backend/<device>/` / 注册分发）与 `srt/platforms/` 平台抽象；PyTorch dispatch key 的反差（**昇腾是 SGLang 树内一等公民，在 PyTorch 眼里却仍走 `PrivateUse1` 外挂通道**）；**华为昇腾**全栈适配逐文件清单（attention/MoE/量化/图捕获/显存池/PD 分离/LoRA + 8 条 CI + 18 篇文档 + 算子开发指南）及容易漏掉的 **CANN + MindSpore** 框架接入线；**摩尔线程 MUSA** 走「重编译 CUDA」路线（回退 `forward_cuda` + 自带 C++ kernel 编译链）；其余厂商的树外插件机制（两个 entry_point group、`SGLANG_PLATFORM` 前置过滤避免 import 无关 SDK、六档 fallback 链）。三条判断：树内深度≈厂商投入、适配分「重写实现」与「重编译」两路线、**树外零命中≠不支持**。仅代码量证据，无性能实测。Updated: 2026-08-19
- [SGLang 架构与调度循环源码走读](学习整理/SGlang.md) — SGLang 三进程模型（Tokenizer/Scheduler/Detokenizer）+ 三层架构走读，逐步拆解 Scheduler 事件循环 recv→分发→组批→run→result：含组批优先级策略（FCFS/LPM/DFS-Weights/LOF…）、采样算法、grammar 约束、eager/CUDA Graph；**RadixCache 与 KV 内存池深挖**——树的增删改查、7 种驱逐策略统一为 `get_priority` 排序键（含级联向上驱逐）、`lock_ref` 如何把前缀移出可驱逐账本、前缀匹配的指数搜索+二分、Token-to-page 两级映射（`req_to_token` 表的 padding 行由来、三个 alloc 入口分工）；**注意力 Backend 深挖**——**Ragged KV vs Paged KV 两种布局的区别与分工**（零间接的连续内存 vs 过页表的离散页、为何 extend 必须两者兼用、分算后按 LSE 合并）、基类 `AttentionBackend` 的三组契约（forward 按 mode 分发 / metadata 三方法与 CUDA Graph 可录制约束 / 能力声明类属性），Triton（CSR 索引 kv_indptr+kv_indices+qo_indptr）、FlashInfer（wrapper + plan/run 两阶段 + IndicesUpdater 适配层 + plan_stream + ragged/paged 双路 `causal` 取值分裂）、TorchNative（逐请求 SDPA 循环）三者实现与横向对比；**算子层 custom_op 注册与多平台分派**——`register_custom_op` / `CustomOpWrapper` / `register_custom_op_from_extern` / `direct_register_custom_op` 四级链条（fake_impl 推形状、mutates_args、eager 与懒注册之争、computed_args 防重编译、按平台挑 dispatch key），以及 `MultiPlatformOp` 构造时绑 `forward_*` 的回退链，并点出两条线对 `torch.compile` 的**相反策略**（藏成黑盒 vs 切回 native 让 Inductor 融）；国产 GPU/NPU 适配已独立成篇（见上条），本篇 §七 只留三个落点与现状概览；含 Detokenizer 增量解码（BPE 边界 / UTF-8 半字符 / trim_matched_stop）+ SSE 流式输出协议。Updated: 2026-08-19

## 推理优化

- [Vibe coding 赋能推理优化](推理优化/Vibe%20coding赋能推理优化.md) — **AI 写代码怎么吃掉推理优化里最贵的人力**，按生成粒度分两级：**算子级 MusaCoder**（摩尔线程 2026-06-10 开源，9B/27B，PyTorch 算子 → CUDA/MUSA 原生 Kernel，KernelBench Overall Pass@8 93.2%；附 KernelBench 分级与 `fast_p` 口径说明，并标出 **⚠️「Pass@8 是正确性不是加速比」的存疑**）与 **系统级 VibeServe**（arXiv 2605.06068，外循环搜设计空间 + 内循环实现/验正确性/压性能，**标准场景仅打平 vLLM、6 个非标场景才胜出**）；收束成一条判据——**这套打法成立靠的是「任务自带裁判」，不是模型变强**，并接上 [国产GPU与NPU适配](学习整理/国产GPU与NPU适配.md) 的「重编译 CUDA」路线。Updated: 2026-09-04
- [量化感知训练（QAT）](推理优化/量化感知训练（QAT）.md) — 从量化基础（r=S(q−z)、对称/粒度、**W×A× 记号与"低比特存储 vs 低比特计算"**：W4A16 每次矩阵乘必反量化、W8A8 免反量化的类型对齐机理）到精度补救梯子（RTN → 校准 PTQ GPTQ/AWQ → QAT 伪量化+STE），再到 Qwen3-0.6B 两阶段实验实证：**位宽扫描**五臂对照（qat vs ptq_ft 才是干净读数）定位 int3 悬崖——PTQ 复读崩溃而 QAT≈原模型（24.0 vs 23.9），int2 双双失效；**int4 主战场**同台——GPTQ 零训练追回 67% 损伤、QAT 同预算再赚 1.6，vLLM+Marlin 实测 0.6B 上 int4 吞吐 −36%（带宽账：权重搬运占比 <20%，"临界尺寸"判断法）；收束成"速度账先行 + 精度梯子逐级爬"的选型方法。Updated: 2026-08-28
- [投机采样](推理优化/投机采样.md) — **按「草稿怎么产生」分三大类**的算法侧总述：先给共同骨架（draft→verify→accept 三步、白送的 bonus token）；**① 草稿模型**——独立 draft 的两个先天问题（小模型难做同一件事、不复用 target 已算出的语义）；**② 辅助层**——Medusa（多解码头 + tree attention + typical acceptance，头间无依赖是其弱点）、EAGLE-1（复用倒数第二层特征的 FeatExtrapolator 自回归外推 + 树状草稿，3 次 forward 猜 10 个 token）、EAGLE-2（动态草稿树）、EAGLE-3（移除 L_fea 释放容量 + 多层特征融合）、MTP（原厂自带、接受率最高但限特定模型）、**DFlash**（target 特征注入 draft 各层 K/V，扩散式并行吐整块）、**DSpark**（Markov Logit-Bias / RNN head 补块内序列依赖 + Confidence Head 预估接受概率供调度）、**DFlash 2**（top-1 85.4% 但 top-16 召回 99.5% → 并行路径选择器只打分相邻对；后缀衰减归因于深层 attention 只剩 8% 关注块内 → two-tap 动态深度卷积；三组 benchmark 含「DSpark 在 27B 上反不如 MTP」的反例）；**③ 数据匹配**——N-gram 与 suffix decoding（零权重成本，只在复读场景有命中率）；末附选型表。配论文示意图。Updated: 2026-09-02
